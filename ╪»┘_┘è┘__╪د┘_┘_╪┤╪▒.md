# دليل نشر نظام إدارة الصيانة والتشغيل كموقع مستقل

الدليل ده هياخدك خطوة بخطوة من الصفر لحد ما يبقى عندك موقع حقيقي بلينك خاص بيك،
شغال برة Claude تمامًا، وبياناته متخزنة في قاعدة بيانات سحابية حقيقية (Firebase).

**الوقت المتوقع:** حوالي 30-45 دقيقة أول مرة.

---

## الجزء الأول: إنشاء قاعدة البيانات (Firebase) — مجانية بالكامل

### 1. اعمل حساب Firebase
1. روح على **console.firebase.google.com**
2. سجّل دخول بحساب Google بتاعك (أو اعمل واحد لو مش عندك)
3. دوس **"Add project"** (أو "إنشاء مشروع")
4. اكتب اسم للمشروع (مثلاً: "نظام-الصيانة") ودوس **Continue**
5. سيبه يعطّل Google Analytics (مش محتاجه) ودوس **Create project**
6. استنى شوية لحد ما يخلص، ودوس **Continue**

### 2. فعّل قاعدة البيانات (Firestore)
1. من القائمة الجانبية شمال، دوس **Build** → **Firestore Database**
2. دوس **Create database**
3. اختار **Start in production mode** ودوس **Next**
4. اختار أقرب منطقة جغرافية (مثلاً `eur3` لو في مصر/الشرق الأوسط) ودوس **Enable**

### 3. ظبط صلاحيات الوصول (مهم جدًا)
1. لسه في نفس صفحة Firestore، دوس تاب **Rules** فوق
2. امسح اللي موجود والصق ده بدله:
```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /maintenance-system/{document} {
      allow read, write: if true;
    }
  }
}
```
3. دوس **Publish**

> ⚠️ **ملحوظة أمان:** القاعدة دي بتخلي أي حد معاه لينك الموقع يقدر يقرا ويعدّل البيانات (زي بالظبط اللي كان شغال في Claude). لو عايز حماية أكتر بعدين (تسجيل دخول بباسورد)، قولّي وهنضيفها في خطوة تانية.

### 4. احصل على بيانات الاتصال (Config Keys)
1. دوس على أيقونة الترس ⚙️ فوق شمال جنب "Project Overview" → **Project settings**
2. انزل تحت لحد **"Your apps"**
3. دوس على أيقونة **`</>`** (Web)
4. اكتب اسم أي حاجة (مثلاً "الموقع") ودوس **Register app**
5. هيظهرلك كود فيه حاجة زي:
```js
const firebaseConfig = {
  apiKey: "AIzaSy...",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abc123"
};
```
6. **انسخ القيم دي كلها** — هتحتاجها في الخطوة الجاية.

---

## الجزء الثاني: ظبط الكود

1. افتح ملف `src/firebase.js` من الملفات اللي بعتهالك
2. استبدل كل "ضع_قيمتك_هنا" بالقيمة المطابقة من اللي نسخته فوق
3. احفظ الملف

---

## الجزء الثالث: النشر على الإنترنت (Vercel) — مجاني بالكامل

### الطريقة الأسهل: رفع مباشر بدون GitHub

1. روح على **vercel.com** وسجّل حساب (تقدر تسجل بحساب Google مباشرة)
2. من الصفحة الرئيسية، دوس **Add New** → **Project**
3. هيطلب منك تربط GitHub — لو معندكش حساب GitHub:
   - اعمل حساب سريع على **github.com** (مجاني)
   - اعمل **repository** جديد فاضي (اسمه مثلاً "maintenance-system")
   - ارفع كل الملفات اللي بعتهالك على الـ repository ده (فيه زرار "Add file" → "Upload files" في صفحة الـ repo، اسحب كل الملفات والمجلدات هناك)
4. ارجع لـ Vercel، دوس **Import** جنب الـ repository اللي عملته
5. Vercel هيتعرف تلقائي إنه مشروع Vite/React، سيب الإعدادات زي ما هي
6. دوس **Deploy**
7. استنى دقيقة أو اتنين، وهيديك **لينك حقيقي** زي:
   `https://maintenance-system-xyz.vercel.app`

**خلاص — ده اللينك بتاعك، شغال برة Claude تمامًا، وأي حد يفتحه من أي جهاز.**

---

## أسئلة متوقعة

**البيانات هتتحفظ فين؟**
في Firebase (قاعدة بيانات جوجل السحابية) — نفس الفكرة اللي كانت شغالة، بس دلوقتي حقيقية ومستقلة تمامًا عن Claude.

**هل ينفع أعمل دومين خاص بيا (زي company.com)؟**
أيوه — من إعدادات المشروع في Vercel فيه خيار "Domains" تقدر تضيف دومين اشتريته من أي مكان (Namecheap, GoDaddy...) وتربطه بالموقع.

**الطباعة هتشتغل دلوقتي؟**
أيوه — بما إن الموقع بقى مستقل تمامًا وشغال في تبويب متصفح عادي (مش جوه أي إطار محمي)، أمر الطباعة المباشر هيشتغل عادي. لو حبيت، قولّي وهرجّع زرار "طباعة" يطبع على طول بدل تحميل ملف.

**لو تهت في أي خطوة؟**
ابعتلي سكرين شوت لأي رسالة خطأ أو خطوة مش فاهمها، وهساعدك خطوة بخطوة.
